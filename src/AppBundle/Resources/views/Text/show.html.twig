{% extends "::base.html.twig" %}

{% block body %}

    <h1>{{ text.title}}</h1>
    
    {{ text.content|markdown }}
    
    <p>
        <a href="{{ path('text_edit', { id: text.id }) }}" class="btn btn-primary">Éditer</a>
    </p>
    
{% endblock %}
